#!/bin/bash
find_dirs() {
  node_modules_dirs=$(find . -name "node_modules" -type d)

  echo "$node_modules_dirs"
}

delete_dirs() {
  for dir in $1; do
    $(rm -rf "$dir")
  done
}

find_dirs

gum confirm && delete_dirs "$node_modules_dirs"
